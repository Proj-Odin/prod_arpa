# Archive

This folder holds material that is not part of the main current workflow.

## Contents

- `old-scripts/` contains superseded script revisions kept for history and comparison
- `extras/` contains side utilities and experiments that are not documented as primary entrypoints

The supported entrypoints remain at the repository root:

- `setup.sh`
- `hdd_validate.sh`
- `setup_conky_overlay.sh`
- `badblocks_state_update.sh`
- `install_badblocks_monitor.sh`
- `install_badblocks_monitor_holistic.sh`